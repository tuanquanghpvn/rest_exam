from django.shortcuts import render

# Create your views here.
# def post_category(request):
#     params = {}
#     data, errors = RequestSchema().load(params)
#     if errors:
#         response 400 (BAD request), errors
#
#     response_contractr = logic_method(data)
#
#     response = ResponseScheme().dump(response_contractr)
#
#     return response(data=response, 200. 204, 201)
